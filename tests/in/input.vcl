# test file

<list> := red | blue=2 | green | beep=Beep();


this is a test = fun;

I want you to beep = Beep();

try color <list> = <$1>;

include "/home/mdl/voice/my_commands/commands/string.vch";


please mix <list> and <list> = $1/$2/$1 Len($1);

range 1..10 = $1;
inline menu (red | blue) = $1;
dictation <_anything> = <$1>;

limited context:
  limited time now = now;
:
